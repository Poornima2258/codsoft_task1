body {
    font-family: sans-serif;
  }
  
  header {
    text-align: center;
  }
  
  h1 {
    font-size: 2em;
  }
  
  section {
    padding: 20px;
  }
  
  .project-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-gap: 20px;
  }
  
  .project-item {
    width: 100%;
    height: 200px;
    object-fit: cover;
  }
  
  footer {
    text-align: center;
    font-size: 0.8em;
  }
